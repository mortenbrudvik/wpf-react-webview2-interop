<Window x:Class="WpfReactApp.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wpf="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="10" >
            <Button Width="150" Command="{Binding AddUserCommand}" Margin="0 0 10 0">Add user</Button>
            <Button Width="150" Command="{Binding RemoveUserCommand}">Remove user</Button>
        </StackPanel>
        
        <ContentControl Grid.Row="1"  Content="{Binding WebView}" />
    </Grid>
</Window>
